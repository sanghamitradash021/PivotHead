import { bootstrapApplication } from '@angular/platform-browser';
import { NgHeadlessPivotComponent } from './public-api';

bootstrapApplication(NgHeadlessPivotComponent).catch(err => console.error(err));
